#!/bin/bash
# Health Notes - iOS Simulator Deployment Wrapper
# This script runs the Python simulator deployment script

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the Python simulator deployment script
python3 "$SCRIPT_DIR/scripts/deploy_to_simulator.py" "$@"
