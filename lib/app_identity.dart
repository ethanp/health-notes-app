/// Single source of truth for product / sync identity.
abstract final class AppIdentity() {
  static const syncAppName = 'health_notes';
  static const displayName = 'Health Notes';
  static const localUserId = 'local';
}
